apiVersion: apps/v1
kind: Deployment
metadata:
  name: virt-site-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: virt-site
  template:
    metadata:
      labels:
        app: virt-site
    spec:
      containers:
      - name: virt-site-container
        image: virt-site:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 80
        resources:
          limits:
            memory: "500Mi"
            cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: virt-site-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
  selector:
    app: virt-site